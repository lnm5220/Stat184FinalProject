---
title: 'Stat 184 Final Project'
author: "Leah Miller"
date: "Due December 16, 2019"
output:
  pdf_document: default
  html_notebook: default
---

## Set up
### Clean up environment and load packages
```{r}
# clean up the RStudio environment 
rm(list = ls())
```

```{r}
library(rvest)
library(tidyverse)
library(lubridate)
```

### Import the data

```{r}
insuranceData = read.csv("insurance.csv")
googleData = read.csv("RegionalInterestByConditionOverTime.csv")
```

### Exploratory Data Analysis
First, I will explore the insurance data.

```{r}
costByRegion <-
  insuranceData %>% 
  ggplot(aes(x=region, y = charges)) +
  geom_point(aes(x=region, y=charges)) +
  #geom_bar(stat="identity", alpha = .2) +
  theme(axis.text.x = element_text(angle = 30, hjust=1))

costByRegion
```



